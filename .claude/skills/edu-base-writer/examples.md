EXAMPLE 1:

BEFORE:
"base": "Article structured data uses Schema.org's Article, NewsArticle, or BlogPosting types to provide search engines with explicit, machine-readable information about your article content. When implemented, this markup helps Google understand page context and can enhance how your articles appear in Google Search, Google News, and Google Assistant.\n\nThe SERP treatment for pages with Article structured data includes enhanced presentation with better titles, larger images, and date information displayed directly in search results. This richer visual treatment helps your content stand out among standard blue link listings.\n\nUnlike many other structured data types, Article schema has no required properties. All properties are recommended. However, the recommended properties carry meaningful weight. The most impactful ones are headline, image (provided in three aspect ratios: 16x9, 4x3, and 1x1, with a minimum resolution of 50,000 pixels), datePublished and dateModified (both in ISO 8601 format with timezone), and author (using Person or Organization type with name and url).\n\nImplementing Article schema on content pages is a low-effort, moderate-reward optimization. The markup is straightforward, the properties map directly to information that already exists on the page, and the potential visibility improvements apply across multiple Google surfaces.",

AFTER:
"base": "Adding Article structured data to your article pages can help Google show better title text, images, and date information in search results. While there aren't any required markup properties for Article pages, including markup that is relevant to your pages can more explicitly tell Google what your content is about.<br><br>Google has some technical guidelines for Article pages:<ul><li>For multi-part articles, the <code>rel=canonical</code> should point at either each individual page or a 'view-all' page (and not to page 1 of a multi-part series).</li><li>If you offer subscription-based access to your website content, or if users must register for access, consider adding structured data for subscription and paywalled content.</li></ul>If you create structured data for an article page, the <code>@type</code> must be one of these three types:<ul><li>Article</li><li>NewsArticle</li><li>BlogPosting</li></ul>Example:<pre><code>{\n  \"@type\": \"BlogPosting\",\n  \"headline\": \"How to Mark Up Article Pages\",\n  \"author\": \"Janae Richards\"\n}</code></pre>",

EXAMPLE 2:
 
BEFORE:
"base": "Google's Article structured data documentation identifies several common mistakes in author markup that prevent Google from properly processing authorship information. Getting author markup right is important because Google uses it to establish content attribution, support E-E-A-T evaluation, and connect authors to the broader knowledge graph.\n\nThe most common mistakes, as identified by Google, are:\n\n<ul>\n<li>Merging multiple authors into a single field. When an article has more than one author, each author must be listed as a separate object in the author array. Writing 'Jane Smith and John Doe' as a single author name prevents Google from identifying either individual.</li>\n<li>Including job titles or honorific prefixes in the author.name field. The name property should contain only the author's name. 'Dr. Jane Smith, MD' or 'Jane Smith, Senior Editor' should be simplified to 'Jane Smith'. Credentials and titles belong elsewhere, such as on the author's profile page.</li>\n<li>Using 'Thing' as the author type. The @type for an author must be either 'Person' or 'Organization'. Using the generic 'Thing' type provides insufficient specificity for Google to process the authorship information.</li>\n<li>Omitting the author.url property. The url property should link to the author's profile page on your site or another authoritative web presence. This URL helps Google disambiguate authors with common names and connects the author entity to other information about them on the web.</li>\n</ul>\n\nEnsure that every author visible in the article byline is also represented in the structured data. Mismatches between visible bylines and schema author data create inconsistencies that can undermine trust signals.",

AFTER:
"base": "Google has some recommended best practices for author markup:<ul><li>Include all of the authors listed on the web page in the markup.</li><li>When specifying multiple authors, list each author in their own <code>author</code> field. Example:<pre><code>{\n  \"author\": [\n    {\"name\": \"Willow Lane\"},\n    {\"name\": \"Saleem Ashkar\"}\n  ]\n}</code></pre></li><li>Help Google better understand the author by using additional fields, such as the <code>@type</code> and <code>url</code> (or <code>sameAs</code>) properties. Example:<pre><code>{\n  \"author\": [\n    {\n      \"@type\": \"Person\",\n      \"name\": \"Willow Lane\",\n      \"url\": \"https://www.example.com/staff/willow_lane\"\n    }\n  ]\n}</code></pre></li><li>Only specify the author's name in the <code>author.name</code> property.</li><li>Use the appropriate <code>@type</code>, e.g., <code>Person</code> for people and <code>Organization</code> for organizations.</li></ul>",