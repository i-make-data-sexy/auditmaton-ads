# blueprints/preferences/__init__.py
# UI preferences blueprint — small key/value store scoped to a user, used
# so view choices (filters, sort orders, etc.) sync across browsers.

from blueprints.preferences.routes import preferences_bp
